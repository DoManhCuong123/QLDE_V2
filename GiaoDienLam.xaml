<UserControl x:Class="QLDE_V2.GiaoDienLam"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLDE_V2"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="350">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" ></RowDefinition>
            <RowDefinition Height="auto" ></RowDefinition>
            <RowDefinition Height="auto" ></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="txtTime" Grid.Column="0" Grid.Row="0" Margin="10,5,10,5"/>
        <Button Content="Hoàn thành bài thi" Click="Button_Click" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2" Margin="10,5,10,5"/>
      
    </Grid>
</UserControl>
